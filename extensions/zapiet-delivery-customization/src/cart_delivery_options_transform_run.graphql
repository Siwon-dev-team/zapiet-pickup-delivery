query CartDeliveryOptionsTransformRunInput {
  cart {
    attribute(key: "_zapiet_method") {
      value
    }
    deliveryGroups {
      deliveryOptions {
        handle
        code
      }
    }
  }
}
